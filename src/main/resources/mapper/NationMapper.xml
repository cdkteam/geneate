<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.logy.mapper.NationMapper">
    <select id="queryAllNation" resultType="com.logy.mode.Nation">
        select
        *
        from
        nationtab
    </select>
</mapper>